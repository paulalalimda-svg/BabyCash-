# =============================================================================
# VARIABLES DE ENTORNO - BabyCash Backend
# =============================================================================
# Este archivo es una PLANTILLA. Copiar a .env y completar con valores reales.
# NUNCA commitear el archivo .env con valores reales a Git.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE - PostgreSQL
# -----------------------------------------------------------------------------
# URL de conexión a PostgreSQL con SSL habilitado
# Formato: jdbc:postgresql://host:puerto/database?sslmode=require&serverTimezone=America/Bogota
DB_URL=jdbc:postgresql://localhost:5432/babycash?sslmode=require&serverTimezone=America/Bogota

# Usuario de la base de datos
DB_USERNAME=postgres

# Contraseña de la base de datos (OBLIGATORIO - nunca dejar en blanco en producción)
DB_PASSWORD=your_secure_database_password_here

# -----------------------------------------------------------------------------
# JWT - Autenticación
# -----------------------------------------------------------------------------
# Secret para firma de tokens JWT
# DEBE ser un string de al menos 256 bits (32 caracteres)
# Generar con: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long_change_this_in_production

# -----------------------------------------------------------------------------
# CORS - Cross-Origin Resource Sharing
# -----------------------------------------------------------------------------
# Dominios permitidos para CORS (separados por coma)
# En desarrollo: http://localhost:3000
# En producción: https://babycash.com,https://www.babycash.com
CORS_ALLOWED_ORIGINS=https://babycash.com,https://www.babycash.com

# -----------------------------------------------------------------------------
# SERVER
# -----------------------------------------------------------------------------
# Puerto del servidor (default: 8080)
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# SWAGGER/OpenAPI
# -----------------------------------------------------------------------------
# Habilitar Swagger UI en producción (default: false)
# Solo activar si es necesario y está protegido con autenticación
SWAGGER_ENABLED=false

# -----------------------------------------------------------------------------
# OPCIONALES - Email (si implementas notificaciones)
# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE EMAIL - OBLIGATORIO PARA RECUPERACIÓN DE CONTRASEÑA

# Email de Gmail desde el que se enviarán los correos
# IMPORTANTE: Debes habilitar 2FA y generar una "Contraseña de Aplicación"
# Tutorial: https://support.google.com/accounts/answer/185833
MAIL_USERNAME=tu-email@gmail.com

# Contraseña de aplicación de Gmail (16 caracteres sin espacios)
# NO uses tu contraseña normal de Gmail, usa una contraseña de aplicación
# Generar en: https://myaccount.google.com/apppasswords
MAIL_PASSWORD=abcdefghijklmnop

# Email que aparecerá en el campo "De" de los correos
MAIL_FROM=noreply@babycash.com

# Email del administrador para notificaciones
MAIL_ADMIN=admin@babycash.com

# URL del frontend (para enlaces en emails de recuperación de contraseña)
# Desarrollo: http://localhost:5173
# Producción: https://tu-dominio.com
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# OPCIONALES - Storage (si implementas subida de imágenes)
# -----------------------------------------------------------------------------
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_S3_BUCKET=babycash-images
# AWS_REGION=us-east-1

# -----------------------------------------------------------------------------
# OPCIONALES - Payment Gateways (cuando integres pagos reales)
# -----------------------------------------------------------------------------
# STRIPE_SECRET_KEY=sk_live_xxxxx
# STRIPE_WEBHOOK_SECRET=whsec_xxxxx
# PAYPAL_CLIENT_ID=xxxxx
# PAYPAL_CLIENT_SECRET=xxxxx
# MERCADOPAGO_ACCESS_TOKEN=xxxxx

# -----------------------------------------------------------------------------
# OPCIONALES - Monitoring
# -----------------------------------------------------------------------------
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
# NEW_RELIC_LICENSE_KEY=xxxxx
# DATADOG_API_KEY=xxxxx

# =============================================================================
# INSTRUCCIONES DE USO
# =============================================================================
#
# 1. DESARROLLO LOCAL:
#    - Copiar este archivo: cp .env.example .env
#    - Completar con valores de desarrollo
#    - El archivo .env está en .gitignore (no se commitea)
#
# 2. PRODUCCIÓN:
#    - NO usar archivo .env en servidor
#    - Configurar variables en el sistema de deployment:
#      * Docker: usar --env-file o docker-compose environment
#      * Kubernetes: usar ConfigMap y Secrets
#      * AWS: usar Parameter Store o Secrets Manager
#      * Heroku: heroku config:set VARIABLE=valor
#      * Azure: Application Settings
#
# 3. GENERAR JWT_SECRET SEGURO:
#    openssl rand -base64 32
#    
#    O en Node.js:
#    node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# 4. VALIDAR VARIABLES:
#    Antes de iniciar la aplicación, verificar que todas las variables
#    obligatorias estén configuradas. La app fallará si falta DB_PASSWORD
#    o JWT_SECRET.
#
# 5. SEGURIDAD:
#    - Rotar JWT_SECRET cada 3-6 meses
#    - Usar contraseñas fuertes (min 16 caracteres, alfanuméricos + símbolos)
#    - Nunca commitear .env a Git
#    - Restringir acceso a variables en servidor (permisos 600)
#    - Usar diferentes secrets en dev/staging/prod
#
# =============================================================================
